"""Automated tests for the Smart CCTV package.

Run all tests from the project root with:

    python3 -m unittest discover -s tests

Note: some tests are skipped when optional system libraries
(opencv, face_recognition, numpy, pygame, ultralytics) are not
installed in the active interpreter. Tests under tests/ that only
exercise pure logic run everywhere.
"""